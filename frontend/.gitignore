# Regras de ignore específicas do Frontend

# Arquivos do Yarn Plug'n'Play
/.pnp
.pnp.js

# Nota: Todas as outras regras comuns (node_modules, build, .env, logs, etc.)
# são gerenciadas pelo arquivo .gitignore na raiz do projeto.